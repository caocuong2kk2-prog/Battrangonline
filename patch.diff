
> diff --git a/user/checkout.html b/user/checkout.html
  index 9353bb3..ffeb3b4 100644
  --- a/user/checkout.html
  +++ b/user/checkout.html
  @@ -4,7 +4,8 @@
   <head>
     <meta charset="UTF-8">
     <meta name="viewport" content="width=device-width, initial-scale=1.0">
  -  <meta name="description" content="Thanh toÃ¡n Ä‘Æ¡n hÃng cá»§a báº¡n â€“ PhÃºc Gia TiÃªn. Nháºp thÃ´ng tin giao 
nháºn hÃng thá»§ cÃ´ng BÃ¡t TrÃng cao cáº¥p.">
  +  <meta name="description"
  +    content="Thanh toÃ¡n Ä‘Æ¡n hÃng cá»§a báº¡n â€“ PhÃºc Gia TiÃªn. Nháºp thÃ´ng tin giao nháºn hÃng thá»§ cÃ´ng 
BÃ¡t TrÃng cao cáº¥p.">
     <title>Thanh ToÃ¡n ÄÆ¡n HÃ ng â€“ PhÃºc Gia TiÃªn</title>
     <link rel="preconnect" href="https://fonts.googleapis.com">
     <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  @@ -17,8 +18,13 @@
     <link rel="icon" href="assets/images/logo.png" type="image/png">
     <style>
       /* Äáº·t ná»n body khá»›p vá»›i section kem, áº©n Ä‘Æ°á»ng káº» header */
  -    body.page-body { background-color: #f8f2ec; }
  -    #site-header { border-bottom-color: transparent !important; }
  +    body.page-body {
  +      background-color: #f8f2ec;
  +    }
  +
  +    #site-header {
  +      border-bottom-color: transparent !important;
  +    }
     </style>
   </head>
   
  @@ -43,7 +49,9 @@
           <div class="checkout-steps" aria-label="CÃ¡c bÆ°á»›c thanh toÃ¡n">
             <div class="checkout-step done" aria-current="false">
               <div class="checkout-step__bubble">
  -              <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" 
stroke-width="2.5"><polyline points="20 6 9 17 4 12"></polyline></svg>
  +              <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
  +                <polyline points="20 6 9 17 4 12"></polyline>
  +              </svg>
               </div>
               <span class="checkout-step__label">Giá» HÃ ng</span>
             </div>
  @@ -64,16 +72,17 @@
       <!-- CHECKOUT CONTENT -->
       <section class="section section--dark" aria-label="Tiáº¿n hÃ nh thanh toÃ¡n">
         <div class="container">
  -        
  +
           <form class="checkout-layout" id="checkout-form" novalidate>
  -          
  +
             <!-- LEFT SIDE - FORMS -->
             <div class="checkout-forms-col">
  -            
  +
               <!-- Shipping Info Card -->
               <div class="checkout-card checkout-card--shipping">
                 <h2 class="checkout-card__title">
  -                <svg class="icon-svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" 
stroke-linecap="round" stroke-linejoin="round">
  +                <svg class="icon-svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"
  +                  stroke-linecap="round" stroke-linejoin="round">
                     <rect x="1" y="3" width="15" height="13" rx="1"></rect>
                     <path d="M16 8h4l3 5v3h-7V8z"></path>
                     <circle cx="5.5" cy="18.5" r="2.5"></circle>
  @@ -81,60 +90,93 @@
                   </svg>
                   ThÃ´ng Tin Giao HÃ ng
                 </h2>
  -              
  +
                 <div class="form-row">
                   <div class="form-group" id="group-name">
                     <label for="shipping-name">Há» vÃ  tÃªn <span class="required">*</span></label>
  -                  <input class="form-control" type="text" id="shipping-name" placeholder="Nguyá»…n VÄƒn A" required 
autocomplete="name">
  +                  <input class="form-control" type="text" id="shipping-name" placeholder="Nguyá»…n VÄƒn A" required
  +                    autocomplete="name">
                     <span class="field-error-msg" id="err-name">Vui lÃ²ng nháº­p há» vÃ  tÃªn.</span>
                   </div>
                   <div class="form-group" id="group-phone">
                     <label for="shipping-phone">Sá»‘ Ä‘iá»‡n thoáº¡i <span class="required">*</span></label>
  -                  <input class="form-control" type="tel" id="shipping-phone" placeholder="0989 123 456" required 
autocomplete="tel">
  +                  <input class="form-control" type="tel" id="shipping-phone" placeholder="0989 123 456" required
  +                    autocomplete="tel">
                     <span class="field-error-msg" id="err-phone">Sá»‘ Ä‘iá»‡n thoáº¡i khÃ´ng há»£p lá»‡ (9â€“12 
sá»‘).</span>
                   </div>
                 </div>
  -              
  +
                 <div class="form-group" id="group-email">
                   <label for="shipping-email">
                     Email (Nháº­n hÃ³a Ä‘Æ¡n)
  -                  <span style="font-weight:400; font-size:0.75em; color:rgba(44,26,12,0.4); text-transform:none; 
letter-spacing:0; margin-left:4px;">(TÃ¹y chá»n)</span>
  +                  <span
  +                    style="font-weight:400; font-size:0.75em; color:rgba(44,26,12,0.4); text-transform:none; 
letter-spacing:0; margin-left:4px;">(TÃ¹y
  +                    chá»n)</span>
                   </label>
  -                <input class="form-control" type="email" id="shipping-email" placeholder="example@gmail.com" 
autocomplete="email">
  +                <input class="form-control" type="email" id="shipping-email" placeholder="example@gmail.com"
  +                  autocomplete="email">
                   <span class="field-error-msg" id="err-email">Email khÃ´ng Ä‘Ãºng Ä‘á»‹nh dáº¡ng (VD: 
ten@gmail.com).</span>
                 </div>
  -              
  +
  +              <div class="form-group" id="group-saved-address" style="display: none; margin-bottom: 24px;">
  +                <label for="saved-address-select">Chá»n tá»« Ä‘á»‹a chá»‰ Ä‘Ã£ lÆ°u</label>
  +                <select class="form-control address-select" id="saved-address-select">
  +                  <option value="">-- Chá»n Ä‘á»‹a chá»‰ --</option>
  +                </select>
  +              </div>
  +
  +              <div class="form-row">
  +                <div class="form-group" id="group-province">
  +                  <label for="shipping-province">Tá»‰nh / ThÃ nh phá»‘ <span class="required">*</span></label>
  +                  <select class="form-control address-select" id="shipping-province" required>
  +                    <option value="">-- Chá»n Tá»‰nh / ThÃ nh phá»‘ --</option>
  +                  </select>
  +                  <span class="field-error-msg" id="err-province">Vui lÃ²ng chá»n Tá»‰nh / ThÃ nh phá»‘.</span>
  +                </div>
  +                <div class="form-group" id="group-ward">
  +                  <label for="shipping-ward">XÃ£ / PhÆ°á»ng / Thá»‹ tráº¥n <span class="required">*</span></label>
  +                  <select class="form-control address-select" id="shipping-ward" required disabled>
  +                    <option value="">-- Chá»n XÃ£ / PhÆ°á»ng --</option>
  +                  </select>
  +                  <span class="field-error-msg" id="err-ward">Vui lÃ²ng chá»n XÃ£ / PhÆ°á»ng.</span>
  +                </div>
  +              </div>
  +
                 <div class="form-group" id="group-address">
  -                <label for="shipping-address">Äá»‹a chá»‰ giao hÃ ng <span class="required">*</span></label>
  -                <input class="form-control" type="text" id="shipping-address" placeholder="Sá»‘ nhÃ, 
ngÃµ/Ä‘Æ°á»ng, phÆ°á»ng/xÃ£, quáºn/huyá»‡n, tá»‰nh/TP" required autocomplete="street-address">
  -                <span class="field-error-msg" id="err-address">Vui lÃ²ng nháº­p Ä‘á»‹a chá»‰ giao hÃ ng.</span>
  +                <label for="shipping-address">Äá»‹a chá»‰ chi tiáº¿t <span class="required">*</span></label>
  +                <input class="form-control" type="text" id="shipping-address" placeholder="Sá»‘ nhÃ, 
ngÃµ/Ä‘Æ°á»ng..."
  +                  required autocomplete="street-address">
  +                <span class="field-error-msg" id="err-address">Vui lÃ²ng nháº­p Ä‘á»‹a chá»‰ chi tiáº¿t.</span>
                 </div>
  -              
  +
                 <div class="form-group">
                   <label for="shipping-notes">Ghi chÃº Ä‘Æ¡n hÃ ng (TÃ¹y chá»n)</label>
  -                <textarea class="form-control" id="shipping-notes" placeholder="Ghi chÃº vá» thá»i gian giao hÃ
ng, yÃªu cáº§u Ä‘Ã³ng gÃ³i quÃ táº·ng..."></textarea>
  +                <textarea class="form-control" id="shipping-notes"
  +                  placeholder="Ghi chÃº vá» thá»i gian giao hÃng, yÃªu cáº§u Ä‘Ã³ng gÃ³i quÃ 
táº·ng..."></textarea>
                 </div>
               </div>
   
               <!-- Payment Methods Card -->
               <div class="checkout-card checkout-card--payment">
                 <h2 class="checkout-card__title">
  -                <svg class="icon-svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" 
stroke-linecap="round" stroke-linejoin="round">
  +                <svg class="icon-svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"
  +                  stroke-linecap="round" stroke-linejoin="round">
                     <rect x="1" y="4" width="22" height="16" rx="2" ry="2"></rect>
                     <line x1="1" y1="10" x2="23" y2="10"></line>
                   </svg>
                   PhÆ°Æ¡ng Thá»©c Thanh ToÃ¡n
                 </h2>
  -              
  +
                 <div class="payment-methods">
  -                
  +
                   <!-- COD Method -->
                   <label class="payment-method-item active" id="method-cod-label">
                     <input type="radio" name="payment_method" value="COD" checked>
                     <div class="payment-method-item__icon">ðŸšš</div>
                     <div class="payment-method-item__info">
                       <span class="payment-method-item__name">Thanh toÃ¡n khi nháº­n hÃ ng (COD)</span>
  -                    <span class="payment-method-item__desc">Thanh toÃ¡n báº±ng tiá»n máº·t khi shipper giao hÃng 
táºn nÆ¡i. An toÃn, khÃ´ng cáº§n tÃi khoáº£n ngÃ¢n hÃng.</span>
  +                    <span class="payment-method-item__desc">Thanh toÃ¡n báº±ng tiá»n máº·t khi shipper giao hÃng 
táºn nÆ¡i. An
  +                      toÃ n, khÃ´ng cáº§n tÃ i khoáº£n ngÃ¢n hÃ ng.</span>
                     </div>
                   </label>
                 </div>
  @@ -145,19 +187,20 @@
             <aside class="checkout-summary-col" aria-label="ÄÆ¡n hÃ ng cá»§a báº¡n">
               <div class="checkout-card checkout-summary-card">
                 <h2 class="checkout-card__title" style="border-bottom: none; margin-bottom: var(--space-4);">
  -                <svg class="icon-svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" 
stroke-linecap="round" stroke-linejoin="round">
  +                <svg class="icon-svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"
  +                  stroke-linecap="round" stroke-linejoin="round">
                     <circle cx="9" cy="21" r="1"></circle>
                     <circle cx="20" cy="21" r="1"></circle>
                     <path d="M1 1h4l2.68 13.39a2 2 0 0 0 2 1.61h9.72a2 2 0 0 0 2-1.61L23 6H6"></path>
                   </svg>
                   ÄÆ¡n HÃ ng Cá»§a Báº¡n
                 </h2>
  -              
  +
                 <!-- Items Summary Grid -->
                 <div class="checkout-summary-items" id="summary-items-list">
                   <!-- populated dynamically -->
                 </div>
  -              
  +
                 <!-- Subtotals -->
                 <div class="checkout-summary-pricing">
                   <div class="checkout-summary-pricing__row">
  @@ -169,13 +212,13 @@
                     <span style="color: var(--color-accent);">Miá»…n phÃ­</span>
                   </div>
                 </div>
  -              
  +
                 <!-- Grand Total -->
                 <div class="checkout-summary-total">
                   <span class="checkout-summary-total__label">Tá»•ng thanh toÃ¡n</span>
                   <span class="checkout-summary-total__val" id="summary-total">0â‚«</span>
                 </div>
  -              
  +
                 <button type="submit" class="btn-place-order" id="btn-submit-order">
                   <div class="btn-spinner"></div>
                   <span class="btn-text">XÃC NHáº¬N Äáº¶T HÃ€NG</span>
  @@ -185,25 +228,40 @@
                 </button>
   
                 <p class="checkout-privacy-policy-text">
  -                Dá»¯ liá»‡u cÃ¡ nhÃ¢n cá»§a báº¡n sáº½ Ä‘Æ°á»£c sá» dá»¥ng Ä‘á»ƒ xá» lÃ½ Ä‘Æ¡n Ä‘áº·t hÃng, há»— 
trá»£ tráº£i nghiá»‡m cá»§a báº¡n trÃªn trang web nÃy vÃ cho cÃ¡c má»¥c Ä‘Ãch khÃ¡c Ä‘Æ°á»£c mÃ´ táº£ trong <a 
href="privacy-policy" target="_blank" class="privacy-link">chÃnh sÃ¡ch riÃªng tÆ°</a> cá»§a chÃºng tÃ´i.
  +                Dá»¯ liá»‡u cÃ¡ nhÃ¢n cá»§a báº¡n sáº½ Ä‘Æ°á»£c sá» dá»¥ng Ä‘á»ƒ xá» lÃ½ Ä‘Æ¡n Ä‘áº·t hÃng, há»— 
trá»£ tráº£i nghiá»‡m cá»§a báº¡n trÃªn trang web
  +                nÃy vÃ cho cÃ¡c má»¥c Ä‘Ãch khÃ¡c Ä‘Æ°á»£c mÃ´ táº£ trong <a href="privacy-policy" target="_blank"
  +                  class="privacy-link">chÃ­nh sÃ¡ch riÃªng tÆ°</a> cá»§a chÃºng tÃ´i.
                 </p>
   
                 <!-- Trust Badges -->
                 <div class="checkout-trust-badges">
                   <div class="checkout-trust-badge">
  -                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" 
stroke-linecap="round" stroke-linejoin="round"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path></svg>
  +                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"
  +                    stroke-linejoin="round">
  +                    <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
  +                  </svg>
                     <span>Thanh toÃ¡n an toÃ n & báº£o máº­t 100%</span>
                   </div>
                   <div class="checkout-trust-badge">
  -                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" 
stroke-linecap="round" stroke-linejoin="round"><rect x="1" y="3" width="15" height="13" rx="1"></rect><path d="M16 
8h4l3 5v3h-7V8z"></path><circle cx="5.5" cy="18.5" r="2.5"></circle><circle cx="18.5" cy="18.5" r="2.5"></circle></svg>
  +                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"
  +                    stroke-linejoin="round">
  +                    <rect x="1" y="3" width="15" height="13" rx="1"></rect>
  +                    <path d="M16 8h4l3 5v3h-7V8z"></path>
  +                    <circle cx="5.5" cy="18.5" r="2.5"></circle>
  +                    <circle cx="18.5" cy="18.5" r="2.5"></circle>
  +                  </svg>
                     <span>Giao hÃ ng COD toÃ n quá»‘c, miá»…n phÃ­</span>
                   </div>
                   <div class="checkout-trust-badge">
  -                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" 
stroke-linecap="round" stroke-linejoin="round"><polyline points="1 4 1 10 7 10"></polyline><path d="M3.51 15a9 9 0 1 0 
.49-4.5"></path></svg>
  +                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"
  +                    stroke-linejoin="round">
  +                    <polyline points="1 4 1 10 7 10"></polyline>
  +                    <path d="M3.51 15a9 9 0 1 0 .49-4.5"></path>
  +                  </svg>
                     <span>Äá»•i tráº£ miá»…n phÃ­ trong 7 ngÃ y</span>
                   </div>
                 </div>
  -              
  +
               </div>
             </aside>
   
  @@ -215,20 +273,31 @@
     </main>
   
     <!-- Lightbox Modal (Checkout) -->
  -  <div id="co-lb" style="position:fixed;inset:0;z-index:99999;display:none;background:rgba(0,0,0,0.92);flex-directio
n:column;align-items:center;justify-content:center;overflow:hidden;">
  +  <div id="co-lb"
  +    style="position:fixed;inset:0;z-index:99999;display:none;background:rgba(0,0,0,0.92);flex-direction:column;align
-items:center;justify-content:center;overflow:hidden;">
       <!-- Toolbar -->
  -    <div style="position:absolute;top:0;left:0;right:0;height:56px;display:flex;align-items:center;justify-content:s
pace-between;padding:0 16px;z-index:2;background:linear-gradient(to 
bottom,rgba(0,0,0,0.65),transparent);pointer-events:none;">
  -      <span id="co-lb-counter" 
style="color:rgba(255,255,255,0.7);font-size:14px;font-family:sans-serif;pointer-events:auto;"></span>
  -      <button id="co-lb-close" style="pointer-events:auto;background:rgba(255,255,255,0.12);color:#fff;border:none;w
idth:36px;height:36px;font-size:22px;cursor:pointer;border-radius:50%;display:flex;align-items:center;justify-content:c
enter;">Ã—</button>
  +    <div
  +      style="position:absolute;top:0;left:0;right:0;height:56px;display:flex;align-items:center;justify-content:spac
e-between;padding:0 16px;z-index:2;background:linear-gradient(to 
bottom,rgba(0,0,0,0.65),transparent);pointer-events:none;">
  +      <span id="co-lb-counter"
  +        style="color:rgba(255,255,255,0.7);font-size:14px;font-family:sans-serif;pointer-events:auto;"></span>
  +      <button id="co-lb-close"
  +        style="pointer-events:auto;background:rgba(255,255,255,0.12);color:#fff;border:none;width:36px;height:36px;f
ont-size:22px;cursor:pointer;border-radius:50%;display:flex;align-items:center;justify-content:center;">Ã—</button>
       </div>
       <!-- Arrows -->
  -    <button id="co-lb-prev" style="position:absolute;left:12px;top:50%;transform:translateY(-50%);background:rgba(0,
0,0,0.5);color:#fff;border:none;width:46px;height:46px;font-size:22px;border-radius:50%;cursor:pointer;display:none;ali
gn-items:center;justify-content:center;z-index:2;">â®</button>
  -    <button id="co-lb-next" style="position:absolute;right:12px;top:50%;transform:translateY(-50%);background:rgba(0
,0,0,0.5);color:#fff;border:none;width:46px;height:46px;font-size:22px;border-radius:50%;cursor:pointer;display:none;al
ign-items:center;justify-content:center;z-index:2;">â¯</button>
  +    <button id="co-lb-prev"
  +      style="position:absolute;left:12px;top:50%;transform:translateY(-50%);background:rgba(0,0,0,0.5);color:#fff;bo
rder:none;width:46px;height:46px;font-size:22px;border-radius:50%;cursor:pointer;display:none;align-items:center;justif
y-content:center;z-index:2;">â®</button>
  +    <button id="co-lb-next"
  +      style="position:absolute;right:12px;top:50%;transform:translateY(-50%);background:rgba(0,0,0,0.5);color:#fff;b
order:none;width:46px;height:46px;font-size:22px;border-radius:50%;cursor:pointer;display:none;align-items:center;justi
fy-content:center;z-index:2;">â¯</button>
       <!-- Hint -->
  -    <div style="position:absolute;bottom:16px;left:50%;transform:translateX(-50%);color:rgba(255,255,255,0.4);font-s
ize:11px;font-family:sans-serif;pointer-events:none;white-space:nowrap;">LÄƒn chuá»™t Ä‘á»ƒ zoom â€¢ Nháº¥p Ä‘Ã´i 
Ä‘á»ƒ reset</div>
  +    <div
  +      style="position:absolute;bottom:16px;left:50%;transform:translateX(-50%);color:rgba(255,255,255,0.4);font-size
:11px;font-family:sans-serif;pointer-events:none;white-space:nowrap;">
  +      LÄƒn chuá»™t Ä‘á»ƒ zoom â€¢ Nháº¥p Ä‘Ã´i Ä‘á»ƒ reset</div>
       <!-- Viewport -->
  -    <div id="co-lb-vp" style="position:relative;width:90vw;height:88vh;overflow:hidden;display:flex;align-items:cent
er;justify-content:center;">
  -      <div id="co-lb-media" style="display:flex;align-items:center;justify-content:center;transform-origin:center 
center;transition:opacity 0.18s;will-change:transform;"></div>
  +    <div id="co-lb-vp"
  +      style="position:relative;width:90vw;height:88vh;overflow:hidden;display:flex;align-items:center;justify-conten
t:center;">
  +      <div id="co-lb-media"
  +        style="display:flex;align-items:center;justify-content:center;transform-origin:center 
center;transition:opacity 0.18s;will-change:transform;">
  +      </div>
       </div>
     </div>
   
  @@ -236,15 +305,16 @@
     <div id="footer-placeholder"></div>
   
     <script src="js/api.js"></script>
  +  <script src="js/auth.js"></script>
     <script src="js/promo.js?v=8"></script>
     <script src="js/common.js"></script>
     <script src="js/cart.js"></script>
  -  
  +
     <script>
       // ============================================
       // CHECKOUT PAGE CONTROLLER
       // ============================================
  -    (function() {
  +    (function () {
         'use strict';
   
         var isSubmitting = false;
  @@ -261,23 +331,264 @@
         }
   
         function clearErrors() {
  -        document.querySelectorAll('.form-group.has-error').forEach(function(g) {
  +        document.querySelectorAll('.form-group.has-error').forEach(function (g) {
             g.classList.remove('has-error');
           });
         }
   
  +      // â”€â”€ Custom Select System â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
  +      function initCustomSelect(selectEl) {
  +        if (!selectEl) return;
  +        if (selectEl.nextElementSibling && selectEl.nextElementSibling.classList.contains('custom-select-trigger')) 
{
  +          updateCustomSelect(selectEl);
  +          return;
  +        }
  +
  +        var wrapper = document.createElement('div');
  +        wrapper.className = 'custom-select-wrapper';
  +        if (selectEl.disabled) wrapper.classList.add('disabled');
  +
  +        selectEl.parentNode.insertBefore(wrapper, selectEl);
  +        wrapper.appendChild(selectEl);
  +
  +        var trigger = document.createElement('div');
  +        trigger.className = 'custom-select-trigger';
  +        trigger.innerHTML = '<span class=\"custom-select-text\">' + 
(selectEl.options[selectEl.selectedIndex]?.textContent || '') + '</span><div class=\"arrow\"></div>';
  +
  +        var optionsContainer = document.createElement('div');
  +        optionsContainer.className = 'custom-select-options';
  +
  +        var searchBox = document.createElement('div');
  +        searchBox.className = 'custom-select-search';
  +        searchBox.innerHTML = '<input type=\"text\" placeholder=\"TÃ¬m kiáº¿m...\">';
  +        optionsContainer.appendChild(searchBox);
  +
  +        var optionsList = document.createElement('div');
  +        optionsList.className = 'custom-select-list';
  +        optionsContainer.appendChild(optionsList);
  +
  +        wrapper.appendChild(trigger);
  +        wrapper.appendChild(optionsContainer);
  +
  +        function renderOptions(filter) {
  +          optionsList.innerHTML = '';
  +          var hasVisible = false;
  +          Array.from(selectEl.options).forEach(function (opt, index) {
  +            if (index === 0 && !opt.value) return;
  +            if (filter && opt.textContent.toLowerCase().indexOf(filter.toLowerCase()) === -1) return;
  +            hasVisible = true;
  +
  +            var optionEl = document.createElement('div');
  +            optionEl.className = 'custom-select-option' + (selectEl.selectedIndex === index ? ' selected' : '');
  +            optionEl.textContent = opt.textContent;
  +            optionEl.dataset.value = opt.value;
  +
  +            optionEl.addEventListener('click', function (e) {
  +              e.stopPropagation();
  +              selectEl.value = opt.value;
  +              selectEl.dispatchEvent(new Event('change'));
  +              trigger.querySelector('.custom-select-text').textContent = opt.textContent;
  +              wrapper.classList.remove('open');
  +
  +              var formGroup = wrapper.closest('.form-group');
  +              if (formGroup) formGroup.classList.remove('has-error');
  +            });
  +            optionsList.appendChild(optionEl);
  +          });
  +          if (!hasVisible) {
  +            var empty = document.createElement('div');
  +            empty.className = 'custom-select-option';
  +            empty.style.color = '#999';
  +            empty.textContent = 'KhÃ´ng tÃ¬m tháº¥y káº¿t quáº£';
  +            optionsList.appendChild(empty);
  +          }
  +        }
  +
  +        renderOptions();
  +
  +        trigger.addEventListener('click', function (e) {
  +          e.stopPropagation();
  +          if (selectEl.disabled) return;
  +
  +          var isOpen = wrapper.classList.contains('open');
  +          document.querySelectorAll('.custom-select-wrapper').forEach(function (w) { w.classList.remove('open'); });
  +
  +          if (!isOpen) {
  +            wrapper.classList.add('open');
  +            var input = searchBox.querySelector('input');
  +            input.value = '';
  +            renderOptions();
  +            setTimeout(function () { input.focus(); }, 50);
  +          }
  +        });
  +
  +        searchBox.querySelector('input').addEventListener('input', function (e) {
  +          renderOptions(e.target.value);
  +        });
  +        searchBox.addEventListener('click', function (e) { e.stopPropagation(); });
  +
  +        document.addEventListener('click', function (e) {
  +          if (!wrapper.contains(e.target)) {
  +            wrapper.classList.remove('open');
  +          }
  +        });
  +      }
  +
  +      function updateCustomSelect(selectEl) {
  +        if (!selectEl) return;
  +        var wrapper = selectEl.closest('.custom-select-wrapper');
  +        if (!wrapper) {
  +          initCustomSelect(selectEl);
  +          return;
  +        }
  +
  +        if (selectEl.disabled) wrapper.classList.add('disabled');
  +        else wrapper.classList.remove('disabled');
  +
  +        var trigger = wrapper.querySelector('.custom-select-trigger .custom-select-text');
  +        if (trigger) trigger.textContent = selectEl.options[selectEl.selectedIndex]?.textContent || '';
  +      }
  +
  +      // â”€â”€ Address System â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
  +      var savedAddresses = [];
  +
  +      function initAddressSystem() {
  +        var provinceSelect = document.getElementById('shipping-province');
  +        var wardSelect = document.getElementById('shipping-ward');
  +        var savedSelect = document.getElementById('saved-address-select');
  +        var savedGroup = document.getElementById('group-saved-address');
  +        var detailInput = document.getElementById('shipping-address');
  +        var nameInput = document.getElementById('shipping-name');
  +        var phoneInput = document.getElementById('shipping-phone');
  +
  +        // Load provinces
  +        if (window.PhucGiaTienAPI) {
  +          fetch(window.PhucGiaTienAPI.apiBase + '/Address/provinces')
  +            .then(function (res) { return res.json(); })
  +            .then(function (provinces) {
  +              provinces.forEach(function (p) {
  +                var opt = document.createElement('option');
  +                opt.value = p.code;
  +                opt.textContent = p.name;
  +                provinceSelect.appendChild(opt);
  +              });
  +              initCustomSelect(provinceSelect);
  +
  +              // After loading provinces, try to load saved address for guest
  +              var guestAddress = localStorage.getItem('pgt_last_address');
  +              if (guestAddress && !window.AuthService?.getCurrentUser()) {
  +                try {
  +                  var data = JSON.parse(guestAddress);
  +                  provinceSelect.value = data.provinceCode;
  +                  updateCustomSelect(provinceSelect);
  +                  loadWards(data.provinceCode, data.wardCode);
  +                  detailInput.value = data.detailAddress;
  +                } catch (e) { }
  +              }
  +            })
  +            .catch(function (err) { console.error('Error loading provinces', err); });
  +        }
  +
  +        // Load saved addresses if logged in
  +        if (window.AuthService && window.AuthService.getCurrentUser()) {
  +          var token = localStorage.getItem('access_token') || sessionStorage.getItem('access_token');
  +          if (token) {
  +            fetch(window.PhucGiaTienAPI.apiBase + '/Address/saved', {
  +              headers: { 'Authorization': 'Bearer ' + token }
  +            })
  +              .then(function (res) { return res.json(); })
  +              .then(function (addresses) {
  +                if (addresses && addresses.length > 0) {
  +                  savedAddresses = addresses;
  +                  savedGroup.style.display = 'block';
  +                  addresses.forEach(function (a) {
  +                    var opt = document.createElement('option');
  +                    opt.value = a.id;
  +                    opt.textContent = (a.label ? '[' + a.label + '] ' : '') + a.fullAddress;
  +                    savedSelect.appendChild(opt);
  +
  +                    if (a.isDefault) {
  +                      savedSelect.value = a.id;
  +                      applySavedAddress(a);
  +                    }
  +                  });
  +                  initCustomSelect(savedSelect);
  +                }
  +              })
  +              .catch(function (err) { console.error('Error loading saved addresses', err); });
  +          }
  +        }
  +
  +        // Event listeners
  +        provinceSelect.addEventListener('change', function () {
  +          var provinceCode = this.value;
  +          wardSelect.innerHTML = '<option value="">-- Chá»n XÃ£ / PhÆ°á»ng --</option>';
  +          wardSelect.disabled = true;
  +          updateCustomSelect(wardSelect);
  +
  +          if (provinceCode) {
  +            loadWards(provinceCode);
  +          }
  +        });
  +
  +        savedSelect.addEventListener('change', function () {
  +          var id = parseInt(this.value, 10);
  +          if (id) {
  +            var addr = savedAddresses.find(function (a) { return a.id === id; });
  +            if (addr) applySavedAddress(addr);
  +          } else {
  +            // Reset fields
  +            provinceSelect.value = '';
  +            wardSelect.innerHTML = '<option value="">-- Chá»n XÃ£ / PhÆ°á»ng --</option>';
  +            wardSelect.disabled = true;
  +            detailInput.value = '';
  +            updateCustomSelect(provinceSelect);
  +            updateCustomSelect(wardSelect);
  +          }
  +        });
  +
  +        function loadWards(provinceCode, selectedWardCode) {
  +          fetch(window.PhucGiaTienAPI.apiBase + '/Address/wards/' + provinceCode)
  +            .then(function (res) { return res.json(); })
  +            .then(function (wards) {
  +              wardSelect.innerHTML = '<option value="">-- Chá»n XÃ£ / PhÆ°á»ng --</option>';
  +              wards.forEach(function (w) {
  +                var opt = document.createElement('option');
  +                opt.value = w.code;
  +                opt.textContent = w.name;
  +                wardSelect.appendChild(opt);
  +              });
  +              wardSelect.disabled = false;
  +              if (selectedWardCode) {
  +                wardSelect.value = selectedWardCode;
  +              }
  +              initCustomSelect(wardSelect);
  +            })
  +            .catch(function (err) { console.error('Error loading wards', err); });
  +        }
  +
  +        function applySavedAddress(addr) {
  +          if (nameInput && !nameInput.value) nameInput.value = addr.recipientName;
  +          if (phoneInput && !phoneInput.value) phoneInput.value = addr.phone;
  +          provinceSelect.value = addr.provinceCode;
  +          updateCustomSelect(provinceSelect);
  +          loadWards(addr.provinceCode, addr.wardCode);
  +          detailInput.value = addr.detailAddress;
  +        }
  +      }
  +
         // â”€â”€ Init 
â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
         function initCheckout() {
           if (!window.CartAPI) return;
  -        
  +
           var cart = window.CartAPI.getCart();
  -        var selectedItems = cart.filter(function(i) { return i.selected; });
  +        var selectedItems = cart.filter(function (i) { return i.selected; });
   
           if (selectedItems.length === 0) {
             if (typeof window.showToast === 'function') {
               window.showToast('Giá» hÃ ng trá»‘ng hoáº·c chÆ°a chá»n sáº£n pháº©m Ä‘á»ƒ thanh toÃ¡n.', 'error');
             }
  -          setTimeout(function() { window.location.href = "cart"; }, 1500);
  +          setTimeout(function () { window.location.href = "cart"; }, 1500);
             return;
           }
   
  @@ -288,7 +599,7 @@
           } else {
             try {
               user = JSON.parse(localStorage.getItem('current_user') || 'null');
  -          } catch(e) {}
  +          } catch (e) { }
           }
   
           if (user) {
  @@ -335,22 +646,26 @@
             Promise.all(healPromises).then(function () {
               renderSummary(selectedItems);
               setupFormSubmit(selectedItems);
  +            initAddressSystem();
             });
           } else {
             renderSummary(selectedItems);
             setupFormSubmit(selectedItems);
  +          initAddressSystem();
           }
   
           // Real-time clear error on input
  -        ['shipping-name', 'shipping-phone', 'shipping-email', 'shipping-address'].forEach(function(id) {
  +        ['shipping-name', 'shipping-phone', 'shipping-email', 'shipping-address', 'shipping-province', 
'shipping-ward'].forEach(function (id) {
             var el = document.getElementById(id);
             if (el) {
  -            el.addEventListener('input', function() {
  +            el.addEventListener('input', function () {
                 var groupMap = {
                   'shipping-name': 'group-name',
                   'shipping-phone': 'group-phone',
                   'shipping-email': 'group-email',
  -                'shipping-address': 'group-address'
  +                'shipping-address': 'group-address',
  +                'shipping-province': 'group-province',
  +                'shipping-ward': 'group-ward'
                 };
                 setFieldError(groupMap[id], false);
               });
  @@ -361,8 +676,8 @@
         // â”€â”€ Render summary â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
         function renderSummary(items) {
           var listEl = document.getElementById('summary-items-list');
  -        var subtotal = items.reduce(function(s, i) { return s + i.price * i.qty; }, 0);
  -        
  +        var subtotal = items.reduce(function (s, i) { return s + i.price * i.qty; }, 0);
  +
           var total = subtotal;
   
           // Helper: build thumbnail HTML â€“ handles image and video sources
  @@ -381,14 +696,14 @@
   
           // Render item rows
           if (listEl) {
  -          listEl.innerHTML = items.map(function(item) {
  +          listEl.innerHTML = items.map(function (item) {
               var giftHtml = '';
               if (item.gifts && item.gifts.length > 0) {
                 giftHtml = '<div style="margin-top:8px; padding:6px 8px; border-radius:4px; 
background:rgba(200,146,42,0.05); border:1px dashed rgba(200,146,42,0.3);">';
  -              item.gifts.forEach(function(g) {
  +              item.gifts.forEach(function (g) {
                   var gQty = g.quantity ? (g.quantity * item.qty) : item.qty;
                   giftHtml += '<div style="font-size:11px; color:#8b5a2b; display:flex; align-items:center; gap:6px; 
margin-bottom:4px;">';
  -                giftHtml += '<img src="' + (g.imageUrl || 'assets/images/placeholder.png') + '" style="width:20px; 
height:20px; object-fit:cover; border-radius:3px; border:1px solid rgba(200,146,42,0.2); cursor:zoom-in;" 
onclick="if(window.showImageModal) window.showImageModal(\'' + (g.imageUrl || 'assets/images/placeholder.png') + '\', 
\'' + g.name.replace(/'/g, "\\'") + '\')">';
  +                giftHtml += '<img src="' + (g.imageUrl || 'assets/images/placeholder.webp') + '" style="width:20px; 
height:20px; object-fit:cover; border-radius:3px; border:1px solid rgba(200,146,42,0.2); cursor:zoom-in;" 
onclick="if(window.showImageModal) window.showImageModal(\'' + (g.imageUrl || 'assets/images/placeholder.webp') + '\', 
\'' + g.name.replace(/'/g, "\\'") + '\')">';
                   giftHtml += '<span style="line-height:1.2; flex-grow:1;">QuÃ táº·ng: <strong>' + g.name + 
'</strong> <b style="color:#d32f2f; margin-left:2px;">x' + gQty + '</b></span>';
                   giftHtml += '</div>';
                 });
  @@ -397,13 +712,13 @@
   
               return [
                 '<div class="checkout-item-row">',
  -                buildThumb(item.image, item.name),
  -                '<div class="checkout-item-row__info">',
  -                  '<a href="product-detail.html?id=' + item.id + '" target="_blank" class="checkout-item-row__name" 
style="text-decoration: none; display: block; transition: color 0.2s;" 
onmouseover="this.style.color=\'var(--color-accent)\'" onmouseout="this.style.color=\'\'">' + (item.size ? item.name + 
' - ' + item.size : item.name) + '</a>',
  -                  '<p class="checkout-item-row__qty">Sá»‘ lÆ°á»£ng: <strong>' + item.qty + '</strong></p>',
  -                  giftHtml,
  -                '</div>',
  -                '<span class="checkout-item-row__price">' + window.formatVND(item.price * item.qty) + '</span>',
  +              buildThumb(item.image, item.name),
  +              '<div class="checkout-item-row__info">',
  +              '<a href="product-detail.html?id=' + item.id + '" target="_blank" class="checkout-item-row__name" 
style="text-decoration: none; display: block; transition: color 0.2s;" 
onmouseover="this.style.color=\'var(--color-accent)\'" onmouseout="this.style.color=\'\'">' + (item.size ? item.name + 
' - ' + item.size : item.name) + '</a>',
  +              '<p class="checkout-item-row__qty">Sá»‘ lÆ°á»£ng: <strong>' + item.qty + '</strong></p>',
  +              giftHtml,
  +              '</div>',
  +              '<span class="checkout-item-row__price">' + window.formatVND(item.price * item.qty) + '</span>',
                 '</div>'
               ].join('');
             }).join('');
  @@ -422,7 +737,7 @@
           var form = document.getElementById('checkout-form');
           var submitBtn = document.getElementById('btn-submit-order');
   
  -        form.addEventListener('submit', function(e) {
  +        form.addEventListener('submit', function (e) {
             e.preventDefault();
   
             if (isSubmitting) return;
  @@ -430,10 +745,19 @@
             var name = document.getElementById('shipping-name').value.trim();
             var phone = document.getElementById('shipping-phone').value.trim();
             var email = document.getElementById('shipping-email').value.trim();
  -          var address = document.getElementById('shipping-address').value.trim();
             var notes = document.getElementById('shipping-notes').value.trim();
             var method = 'COD'; // Forced to COD only
   
  +          var provinceSelect = document.getElementById('shipping-province');
  +          var wardSelect = document.getElementById('shipping-ward');
  +          var detailInput = document.getElementById('shipping-address');
  +
  +          var provinceCode = provinceSelect.value;
  +          var provinceName = provinceSelect.options[provinceSelect.selectedIndex]?.text || '';
  +          var wardCode = wardSelect.value;
  +          var wardName = wardSelect.options[wardSelect.selectedIndex]?.text || '';
  +          var detailAddress = detailInput.value.trim();
  +
             // Inline validation
             clearErrors();
             var hasError = false;
  @@ -446,7 +770,9 @@
             var emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
             if (email && !emailRegex.test(email)) { setFieldError('group-email', true); hasError = true; }
   
  -          if (!address) { setFieldError('group-address', true); hasError = true; }
  +          if (!provinceCode) { setFieldError('group-province', true); hasError = true; }
  +          if (!wardCode) { setFieldError('group-ward', true); hasError = true; }
  +          if (!detailAddress) { setFieldError('group-address', true); hasError = true; }
   
             if (hasError) {
               // Scroll to first error
  @@ -458,6 +784,8 @@
               return;
             }
   
  +          var fullAddress = detailAddress + ', ' + wardName + ', ' + provinceName;
  +
             // Loading state
             isSubmitting = true;
             if (submitBtn) {
  @@ -466,7 +794,7 @@
             }
   
             // Build order data
  -          var subtotal = items.reduce(function(s, i) { return s + i.price * i.qty; }, 0);
  +          var subtotal = items.reduce(function (s, i) { return s + i.price * i.qty; }, 0);
             var total = subtotal;
   
             // Prepare payload for backend API
  @@ -474,9 +802,9 @@
               customer: name,
               phone: phone.replace(/\D/g, ''),
               email: email || '',
  -            address: address,
  +            address: fullAddress,
               customerNote: notes || null,
  -            items: items.map(function(i) {
  +            items: items.map(function (i) {
                 return {
                   id: parseInt(i.id, 10),
                   size: i.size || null,
  @@ -485,112 +813,29 @@
               })
             };
   
  -          // Send to backend API
  -          if (window.PhucGiaTienAPI && typeof window.PhucGiaTienAPI.createOrder === 'function') {
  -            window.PhucGiaTienAPI.createOrder(apiPayload)
  -              .then(function(response) {
  -                var finalOrderCode = response.orderCode || ('PGT' + Math.floor(100000 + Math.random() * 900000));
  -                
  -                var orderData = {
  -                  orderCode: finalOrderCode,
  -                  date: new Date().toISOString(),
  -                  timestamp: Date.now(),
  -                  name: name,
  -                  phone: phone.replace(/\D/g, ''), // chuáº©n hÃ³a SÄT chá»‰ sá»‘
  -                  email: email,
  -                  address: address,
  -                  customerNote: notes,
  -                  paymentMethod: 'Thanh toÃ¡n khi nháº­n hÃ ng (COD)',
  -                  items: items.map(function(i) {
  -                    return { productId: i.id, name: i.name, qty: i.qty, price: i.price, image: i.image, size: 
i.size, gifts: i.gifts };
  -                  }),
  -                  subtotal: subtotal,
  -                  total: total,
  -                  status: 'pending',                              // Tráº¡ng thÃ¡i ban Ä‘áº§u
  -                  statusHistory: [
  -                    { status: 'pending', time: new Date().toISOString(), note: 'ÄÆ¡n hÃng Ä‘Ã£ Ä‘Æ°á»£c tiáº¿p nháº
n thÃnh cÃ´ng.' }
  -                  ]
  -                };
  -
  -                // LÆ°u Ä‘Æ¡n má»›i nháº¥t (cho order-success.html)
  -                localStorage.setItem('pgt_last_order', JSON.stringify(orderData));
  -
  -                // LÆ°u vÃ o máº£ng táº¥t cáº£ Ä‘Æ¡n (cho tra cá»©u)
  -                var allOrders = [];
  -                try { allOrders = JSON.parse(localStorage.getItem('pgt_orders') || '[]'); } catch(e) {}
  -                allOrders.unshift(orderData); // Ä‘Æ¡n má»›i nháº¥t lÃªn Ä‘áº§u
  -                if (allOrders.length > 50) allOrders = allOrders.slice(0, 50); // giá»›i háº¡n 50 Ä‘Æ¡n
  -                localStorage.setItem('pgt_orders', JSON.stringify(allOrders));
  -
  -                // Clear purchased items from cart
  -                if (window.CartAPI) {
  -                  var cart = window.CartAPI.getCart();
  -                  var remaining = cart.filter(function(i) { return !i.selected; });
  -                  localStorage.setItem('pgt_cart', JSON.stringify(remaining));
  -                  document.dispatchEvent(new CustomEvent('cart-updated'));
  -                }
  -
  -                // Clear applied promo
  -                sessionStorage.removeItem('applied_promo_percent');
  -
  -                // Redirect to success page
  -                setTimeout(function() {
  -                  window.location.href = "order-success";
  -                }, 600);
  -              })
  -              .catch(function(err) {
  -                console.error('Order creation error:', err);
  -                isSubmitting = false;
  -                if (submitBtn) {
  -                  submitBtn.classList.remove('loading');
  -                  submitBtn.disabled = false;
  -                }
  -                if (typeof window.showToast === 'function') {
  -                  window.showToast('KhÃ´ng thá»ƒ káº¿t ná»‘i Ä‘áº¿n mÃ¡y chá»§ hoáº·c xá» lÃ½ Ä‘Æ¡n hÃng. Vui lÃ²ng 
thá» láº¡i!', 'error');
  -                } else {
  -                  alert('CÃ³ lá»—i xáº£y ra trong quÃ¡ trÃ¬nh Ä‘áº·t hÃ ng. Vui lÃ²ng thá»­ láº¡i!');
  -                }
  -              });
  -          } else {
  -            // Fallback for standalone frontend mock mode
  -            var orderCode = sessionStorage.getItem('temp_memo_code') || ('PGT' + Math.floor(100000 + Math.random() 
* 900000));
  -            var orderData = {
  -              orderCode: orderCode,
  -              date: new Date().toISOString(),
  -              timestamp: Date.now(),
  -              name: name,
  -              phone: phone.replace(/\D/g, ''),
  -              email: email,
  -              address: address,
  -              customerNote: notes,
  -              paymentMethod: 'Thanh toÃ¡n khi nháº­n hÃ ng (COD)',
  -              items: items.map(function(i) {
  -                return { name: i.name, qty: i.qty, price: i.price, image: i.image, size: i.size, gifts: i.gifts };
  -              }),
  -              subtotal: subtotal,
  -              total: total,
  -              status: 'pending',
  -              statusHistory: [
  -                { status: 'pending', time: new Date().toISOString(), note: 'ÄÆ¡n hÃng Ä‘Ã£ Ä‘Æ°á»£c tiáº¿p nháºn 
thÃnh cÃ´ng.' }
  -              ]
  -            };
  -
  -            localStorage.setItem('pgt_last_order', JSON.stringify(orderData));
  -            var allOrders = [];
  -            try { allOrders = JSON.parse(localStorage.getItem('pgt_orders') || '[]'); } catch(e) {}
  -            allOrders.unshift(orderData);
  -            if (allOrders.length > 50) allOrders = allOrders.slice(0, 50);
  -            localStorage.setItem('pgt_orders', JSON.stringify(allOrders));
  +          // Temporarily save address payload for post-order save prompt
  +          var addrPayload = {
  +            recipientName: name,
  +            phone: phone.replace(/\D/g, ''),
  +            provinceCode: parseInt(provinceCode),
  +            provinceName: provinceName,
  +            wardCode: parseInt(wardCode),
  +            wardName: wardName,
  +            detailAddress: detailAddress,
  +            fullAddress: fullAddress
  +          };
  +          sessionStorage.setItem('pgt_temp_address', JSON.stringify(addrPayload));
   
  +          // Send to backend API
               if (window.CartAPI) {
                 var cart = window.CartAPI.getCart();
  -              var remaining = cart.filter(function(i) { return !i.selected; });
  +              var remaining = cart.filter(function (i) { return !i.selected; });
                 localStorage.setItem('pgt_cart', JSON.stringify(remaining));
                 document.dispatchEvent(new CustomEvent('cart-updated'));
               }
               sessionStorage.removeItem('applied_promo_percent');
   
  -            setTimeout(function() {
  +            setTimeout(function () {
                 window.location.href = "order-success";
               }, 600);
             }
  @@ -599,30 +844,30 @@
   
         // â”€â”€ Checkout Lightbox â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
         function initCheckoutLightbox() {
  -        var lb       = document.getElementById('co-lb');
  -        var lbMedia  = document.getElementById('co-lb-media');
  -        var lbVp     = document.getElementById('co-lb-vp');
  -        var lbClose  = document.getElementById('co-lb-close');
  -        var lbPrev   = document.getElementById('co-lb-prev');
  -        var lbNext   = document.getElementById('co-lb-next');
  -        var lbCnt    = document.getElementById('co-lb-counter');
  +        var lb = document.getElementById('co-lb');
  +        var lbMedia = document.getElementById('co-lb-media');
  +        var lbVp = document.getElementById('co-lb-vp');
  +        var lbClose = document.getElementById('co-lb-close');
  +        var lbPrev = document.getElementById('co-lb-prev');
  +        var lbNext = document.getElementById('co-lb-next');
  +        var lbCnt = document.getElementById('co-lb-counter');
           if (!lb) return;
   
  -        var srcs   = [];
  +        var srcs = [];
           var curIdx = 0;
           var zs = 1, px = 0, py = 0;
           var MIN_Z = 1, MAX_Z = 5, STEP = 0.4;
   
           function clamp() {
             if (zs <= 1) { px = 0; py = 0; return; }
  -          var maxX = Math.max(0, (lbVp.clientWidth  * zs - lbVp.clientWidth)  / 2);
  +          var maxX = Math.max(0, (lbVp.clientWidth * zs - lbVp.clientWidth) / 2);
             var maxY = Math.max(0, (lbVp.clientHeight * zs - lbVp.clientHeight) / 2);
             px = Math.max(-maxX, Math.min(maxX, px));
             py = Math.max(-maxY, Math.min(maxY, py));
           }
           function applyT(anim) {
             lbMedia.style.transition = anim ? 'transform 0.2s ease' : 'none';
  -          lbMedia.style.transform  = 'translate(' + px + 'px,' + py + 'px) scale(' + zs + ')';
  +          lbMedia.style.transform = 'translate(' + px + 'px,' + py + 'px) scale(' + zs + ')';
             lbVp.style.cursor = zs > 1 ? 'grab' : 'zoom-in';
           }
           function resetZ(anim) { zs = 1; px = 0; py = 0; applyT(anim); }
  @@ -638,7 +883,7 @@
             var isVid = /\.(mp4|webm|mov|avi|ogg)$/i.test(src);
             lbMedia.style.opacity = '0';
             resetZ(false);
  -          setTimeout(function() {
  +          setTimeout(function () {
               if (isVid) {
                 lbMedia.innerHTML = '<video src="' + src + '" autoplay loop controls 
style="max-width:85vw;max-height:85vh;border-radius:8px;box-shadow:0 4px 30px 
rgba(0,0,0,0.7);object-fit:contain;display:block;"></video>';
               } else {
  @@ -667,13 +912,13 @@
           // Delegate click on summary list thumbnails
           var summaryList = document.getElementById('summary-items-list');
           if (summaryList) {
  -          summaryList.addEventListener('click', function(e) {
  +          summaryList.addEventListener('click', function (e) {
               var thumb = e.target.closest('[data-co-lb-src]');
               if (!thumb) return;
               // Rebuild src list from all visible thumbnails
               srcs = Array.from(summaryList.querySelectorAll('[data-co-lb-src]'))
  -                        .map(function(el) { return el.getAttribute('data-co-lb-src'); })
  -                        .filter(Boolean);
  +              .map(function (el) { return el.getAttribute('data-co-lb-src'); })
  +              .filter(Boolean);
               var clicked = thumb.getAttribute('data-co-lb-src');
               curIdx = Math.max(0, srcs.indexOf(clicked));
               openLb(curIdx);
  @@ -681,29 +926,29 @@
           }
   
           lbClose.addEventListener('click', closeLb);
  -        lb.addEventListener('click', function(e) { if (e.target === lb) closeLb(); });
  -        if (lbPrev) lbPrev.addEventListener('click', function(e) { e.stopPropagation(); curIdx = (curIdx - 1 + 
srcs.length) % srcs.length; renderLb(); });
  -        if (lbNext) lbNext.addEventListener('click', function(e) { e.stopPropagation(); curIdx = (curIdx + 1) % 
srcs.length; renderLb(); });
  +        lb.addEventListener('click', function (e) { if (e.target === lb) closeLb(); });
  +        if (lbPrev) lbPrev.addEventListener('click', function (e) { e.stopPropagation(); curIdx = (curIdx - 1 + 
srcs.length) % srcs.length; renderLb(); });
  +        if (lbNext) lbNext.addEventListener('click', function (e) { e.stopPropagation(); curIdx = (curIdx + 1) % 
srcs.length; renderLb(); });
   
           // Keyboard
  -        document.addEventListener('keydown', function(e) {
  +        document.addEventListener('keydown', function (e) {
             if (lb.style.display === 'none') return;
             if (e.key === 'Escape') closeLb();
  -          if (e.key === 'ArrowLeft')  { curIdx = (curIdx - 1 + srcs.length) % srcs.length; renderLb(); }
  +          if (e.key === 'ArrowLeft') { curIdx = (curIdx - 1 + srcs.length) % srcs.length; renderLb(); }
             if (e.key === 'ArrowRight') { curIdx = (curIdx + 1) % srcs.length; renderLb(); }
           });
   
           // Scroll-wheel zoom
  -        lbVp.addEventListener('wheel', function(e) {
  +        lbVp.addEventListener('wheel', function (e) {
             e.preventDefault();
             var rect = lbVp.getBoundingClientRect();
  -          var ox = e.clientX - rect.left - rect.width  / 2;
  -          var oy = e.clientY - rect.top  - rect.height / 2;
  +          var ox = e.clientX - rect.left - rect.width / 2;
  +          var oy = e.clientY - rect.top - rect.height / 2;
             zoomTo(zs + (e.deltaY < 0 ? STEP * 0.6 : -STEP * 0.6), ox, oy, false);
           }, { passive: false });
   
           // Double-click zoom
  -        lbVp.addEventListener('dblclick', function(e) {
  +        lbVp.addEventListener('dblclick', function (e) {
             if (zs > 1) { resetZ(true); } else {
               var rect = lbVp.getBoundingClientRect();
               zoomTo(2, e.clientX - rect.left - rect.width / 2, e.clientY - rect.top - rect.height / 2, true);
  @@ -712,17 +957,17 @@
   
           // Drag/pan
           var drag = false, dx, dy, px0, py0;
  -        lbVp.addEventListener('mousedown', function(e) {
  +        lbVp.addEventListener('mousedown', function (e) {
             if (zs <= 1 || e.button !== 0) return;
             drag = true; dx = e.clientX; dy = e.clientY; px0 = px; py0 = py;
             lbVp.style.cursor = 'grabbing'; e.preventDefault();
           });
  -        window.addEventListener('mousemove', function(e) {
  +        window.addEventListener('mousemove', function (e) {
             if (!drag) return;
             px = px0 + (e.clientX - dx); py = py0 + (e.clientY - dy);
             clamp(); applyT(false);
           });
  -        window.addEventListener('mouseup', function() {
  +        window.addEventListener('mouseup', function () {
             if (!drag) return; drag = false;
             lbVp.style.cursor = zs > 1 ? 'grab' : 'zoom-in';
           });
  @@ -730,7 +975,7 @@
   
         // Run on load (wait for cart.js + common.js to init)
         if (document.readyState === 'loading') {
  -        document.addEventListener('DOMContentLoaded', function() {
  +        document.addEventListener('DOMContentLoaded', function () {
             initCheckoutLightbox();
             setTimeout(initCheckout, 650);
           });
  @@ -742,4 +987,4 @@
     </script>
   </body>
   
  -</html>
  +</html>
  \ No newline at end of file


